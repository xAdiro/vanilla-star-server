@page
@model StatsModel
@{
    ViewData["Title"] = "Statystyki";
}
<h2>Statystyki</h2>

Brak danych
@*
@foreach(var board in @Model.Categories){
    <div>@board.Value   @board.FriendlyName</div>
}
*@
